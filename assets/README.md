# App icon

This project keeps the application icon under `assets/`.

If the icon files are missing, regenerate them with:

`python tools/generate_app_icon.py`

Outputs:
- `assets/app_icon.png` (runtime window/app icon)
- `assets/app_icon.ico` (PyInstaller executable icon)

